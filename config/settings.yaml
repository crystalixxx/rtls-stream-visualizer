validation:
  schema_path: "config/schema.json"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"


upd_server:
  ip: "0.0.0.0"
  port: 9999
  topic: "rtls.events"
